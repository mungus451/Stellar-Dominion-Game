# Environment Variables for Stellar Dominion
# Copy this file to .env for local development

# DynamoDB Configuration
DYNAMODB_SESSION_TABLE=starlight-dominion-sessions-local
AWS_REGION=us-east-2

# Aurora Serverless v2 Database Configuration
DB_HOST=starlight-dominion-db.cluster-cl8ugqwekrkc.us-east-2.rds.amazonaws.com
DB_NAME=users

# For local development - use direct credentials
DB_USERNAME=admin
DB_PASSWORD=your_database_password_here

# For Lambda/production - use Secrets Manager (set by serverless.yml)
# DB_SECRET_ARN=arn:aws:secretsmanager:us-east-2:123456789012:secret:starlight-dominion-db-credentials-prod-AbCdEf

# AWS Credentials (for local development only)
# In production/Lambda, these are provided by IAM roles
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Application Environment
APP_ENV=local

# File Storage Configuration
# Supported drivers: local, s3
FILE_STORAGE_DRIVER=local

# Local File Storage Settings (when using local driver)
FILE_STORAGE_LOCAL_PATH=/home/jray/code/Stellar-Dominion-Game/Stellar-Dominion/public/uploads
FILE_STORAGE_LOCAL_URL=/uploads

# S3 File Storage Settings (when using s3 driver)
FILE_STORAGE_S3_BUCKET=your-s3-bucket-name
FILE_STORAGE_S3_REGION=us-east-2
# Optional: Custom S3 URL (for CloudFront or custom domains)
# FILE_STORAGE_S3_URL=https://cdn.yourdomain.com

# Note: When using S3 driver, AWS credentials can be provided via:
# 1. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# 2. IAM roles (recommended for production)
# 3. AWS credentials file
